name = "setup-ssh"
main = "worker.js"
compatibility_date = "2026-01-01"

routes = [
  { pattern = "setup-ssh.muqimjon.uz", custom_domain = true }
]

# Secretlar (wrangler secret put ... yoki dashboard):
#   TS_API_TOKEN   - Tailscale API access token (/ts-key bir martalik key yasaydi)
#   TS_TAILNET     - tailnet nomi (masalan '-')
